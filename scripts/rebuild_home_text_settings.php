<?php
/**
 * Пересобирает storage/site_settings.json с корректной HTML-таблицей в home_text_html.
 * Запуск: php scripts/rebuild_home_text_settings.php
 */
$storage = dirname(__DIR__) . '/storage/site_settings.json';

$intro = <<<'HTML'
<h2>Каталог ленты нержавеющей AISI</h2>
<p>На сайте собраны основные марки нержавеющей ленты AISI по сериям 200/300/400/900L. Здесь удобно подобрать материал под вашу задачу: по марке стали, толщине и ширине, состоянию (мягкая, нагартованная, полугартованная), пружинным свойствам и типу поверхности (BA, 2B, 4N и др.). В карточках товаров указаны ключевые характеристики, наличие и ориентировочные сроки поставки. Если вы не уверены в выборе или нужен аналог — оставьте заявку, мы поможем подобрать подходящую марку и параметры, а также подготовим коммерческое предложение.</p>
HTML;

$rows = [
    [10, '1,0', '0,0793'],
    [20, '1,0', '0,1586'],
    [25, '1,0', '0,1983'],
    [30, '1,0', '0,2379'],
    [40, '1,0', '0,3172'],
    [50, '1,0', '0,3965'],
    [60, '1,0', '0,4758'],
    [80, '1,0', '0,6344'],
    [100, '1,0', '0,7930'],
    [120, '1,0', '0,9516'],
    [150, '1,0', '1,1895'],
    [200, '1,0', '1,5860'],
];

$tbody = '';
foreach ($rows as $r) {
    $tbody .= '<tr><td>' . $r[0] . '</td><td>' . $r[1] . '</td><td>AISI 304</td><td>' . $r[2] . "</td></tr>\n";
}

$rest = <<<HTML

<h3>Расчёт массы и стоимости</h3>
<ul>
<li><strong>Практика расчёта:</strong> стоимость за метр = (теоретическая масса метра) × (цена за кг) + (добавки за сервис/упаковку); НДС и доставка — по условиям договора.</li>
</ul>
<p>Теоретическую массу погонного метра можно оценить по плотности стали марки AISI&nbsp;304 (принято 7,93&nbsp;г/см³, т.&nbsp;е. 7930&nbsp;кг/м³):</p>
<p><strong>толщина (м) × ширина (м) × 7930 (кг/м³)</strong> — масса одного метра ленты в килограммах.</p>
<div class="home-text-table-wrap">
<table class="home-text-table">
<caption>Теоретическая масса 1&nbsp;м ленты при толщине 1&nbsp;мм (AISI&nbsp;304, плотность 7,93&nbsp;г/см³)</caption>
<thead>
<tr>
<th scope="col">Ширина ленты, мм</th>
<th scope="col">Толщина, мм</th>
<th scope="col">Марка (плотность принято 7,93&nbsp;г/см³)</th>
<th scope="col">Теоретическая масса, кг/м</th>
</tr>
</thead>
<tbody>
{$tbody}</tbody>
</table>
</div>
<p class="home-text__note"><small><strong>Примечание:</strong> таблица показывает теоретическую массу для толщины 1&nbsp;мм; фактическая масса партии может отличаться в пределах допусков (ISO&nbsp;9445, ASTM&nbsp;A480 и др.). Для других марок используйте соответствующую плотность (например, у AISI&nbsp;430 плотность иная).</small></p>
HTML;

$html = $intro . $rest;

$data = file_exists($storage) ? (array) json_decode(file_get_contents($storage), true) : [];
$data['home_text_html'] = $html;

file_put_contents(
    $storage,
    json_encode($data, JSON_UNESCAPED_UNICODE | JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES)
);

echo "Written: {$storage} (" . strlen($html) . " bytes HTML)\n";
